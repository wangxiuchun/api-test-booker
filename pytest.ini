[pytest]
markers =
    smoke: 冒烟测试，核心链路，验证系统基本能用
    regression: 完整回归测试，覆盖边界、鉴权、已知缺陷

# 只跑冒烟测试（核心链路）
; pytest -m smoke -v

# 只跑回归测试（边界+鉴权+bug记录）
; pytest -m regression -v

# 全部跑，生成完整报告
; pytest test_cases/ --html=reports/report.html --self-contained-html -v